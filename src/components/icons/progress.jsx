import React from 'react';
import { MdAccessTime } from 'react-icons/md';

const InProgressIcon = ({size,color}) => {
  return (
    <div>
      <MdAccessTime size={size} color={color} />
    </div>
  );
};

export default InProgressIcon;
