import React from 'react';
import { IoSyncOutline } from "react-icons/io5";

const NewIcon = ({color, size }) => {
  return (
    <div>
      <IoSyncOutline size={size} color={color} />
    </div>
  );
};

export default NewIcon;
